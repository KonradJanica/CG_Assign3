#include "car.h"

// Construct with position setting parameters
Car::Car(const glm::vec3 &translation,
               const glm::vec3 &rotation,
               const glm::vec3 &scale,
               float default_speed, bool debugging_on)
{}
